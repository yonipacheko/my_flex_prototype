!!! 5
%html(lang="en-US")
  %body
    %p You are invited by #{@invitation.inviter.full_name} to join
    Myflix!
    = #we need 2 change: password_reset_path to password_reset_url cuz u get this inside an email
    %p= @invitation.message
    - #this routes looks actually like this: /register/:token(.:format)
    - #and belongs to the action called:  'new_with_invitation_token'
    %p= link_to 'Accept this invitation', register_with_token_url(@invitation.token)

